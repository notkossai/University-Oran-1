#include <stdio.h>

int main() {
    int a = 10, b = 20;
    char c = 'Z';
    float f = 3.14;
    double d = 0.5;

    printf("Hello\n");

    if (a < b && b >= 15) {
        a = a + b;
        f = f - 1.0;
    } else {
        a -= 1;
    }

    unsigned int mask = 0x0F;
    mask = mask << 2;
    mask = mask & 0xFF;

    for (int i = 0; i < 3; i++) {
        if (i == 2) break;
    }

    return 0;
}
